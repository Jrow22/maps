
services:
  pg_tileserv:
    image: jamrow/maps:latest
    container_name: maps
    restart: always
    secrets:
      - DATABASE_URL
    ports:
      - "7800:7800"

secrets:
  DATABASE_URL:
    external: true